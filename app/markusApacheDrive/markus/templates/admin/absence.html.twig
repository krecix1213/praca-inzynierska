{% extends "admin/base.html.twig" %}

{% block body %}
    <table class="table">
        <tr><th>Lp.</th><th>Klasa</th><th>#</th></tr>
        {% set i = 1 %}
        {% for v in schoolClass %}
            <tr><td>{{i}}</td><td>{{v.symbol}}</td><td><a href="{{path('absenceClassViewPageAdmin',{id:v.id})}}">Edytuj Nieobecności</a></td></tr>
        {% set i = i + 1 %}
        {% endfor %}
    </table>
{% endblock %}