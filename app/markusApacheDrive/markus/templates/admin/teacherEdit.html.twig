{% extends "admin/base.html.twig" %}

{% block body %}
    <table class="table">
        <tr><th>Lp.</th><th>Klasa</th><th>#</th></tr>
        {% set i = 1 %}
        {% for v in teacher %}
            <tr><td>{{i}}</td><td>{{v.name}} {{v.surname}}</td><td><a href="{{path('TeacherEditAdmin',{id:v.id})}}">Edytuj Nauczyciela</a></td></tr>
        {% set i = i + 1 %}
        {% endfor %}
    </table>
{% endblock %}